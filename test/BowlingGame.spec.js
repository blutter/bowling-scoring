import { BowlingGame } from '../src/BowlingGame';

describe('When a BowlingGame is started', () => {
  const bowlingGame = new BowlingGame();
  
  test('then the initial score is 0', () => {
    expect(true).toBe(true);
  });
});

